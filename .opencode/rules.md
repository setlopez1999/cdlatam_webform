# Reglas del proyecto cd_web_form

- NUNCA ejecutar `npm run dev`, `npm start` ni ningún comando que inicie el servidor. El usuario lo corre manualmente.
- Solo usar `npm run check` para typecheck si necesito verificar.
